<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
     <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
</head>
<body style="background-color: #DCDCDC">

<div class="container mt-3">
    <form name = "autos" action="/updateAuto" method="post" enctype="multipart/form-data" class="mt-3">
        {{#autos}}
            <h3>Изменить</h3>
            <div class="form-group row">
                <label class="col-sm-3 col-form-label">id:</label>
                <div class="col-sm-8">
                    <input disabled type="text" class="form-control" name="id" placeholder="id" value="{{id}}" >
                </div>
            </div>
            <div class="form-group row">
                <label class="col-sm-3 col-form-label">Введите марку:</label>
                <div class="col-sm-8">
                    <input required type="text" class="form-control" name="type" placeholder="Марка" value="{{type}}" >
                </div>
            </div>
            <div class="form-group row">
                <label class="col-sm-3 col-form-label">Введите название модети:</label>
                <div class="col-sm-8">
                    <input required type="text" class="form-control" name="name" placeholder="Название модели" value="{{name}}" >
                </div>
            </div>
            <div class="form-group row">
                <label class="col-sm-3 col-form-label">Введите тех. состояние:</label>
                <div class="col-sm-8">
                    <input required type="text" class="form-control" name="cond" placeholder="Тех. состояние" value="{{cond}}" >
                </div>
            </div>
            <div class="form-group row">
                <label class="col-sm-3 col-form-label">Введите год выпуска:</label>
                <div class="col-sm-8">
                    <input required type="text" class="form-control" name="old" placeholder="Год выпуска" value="{{old}}" >
                </div>
            </div>
            <div class="form-group row">
                <label class="col-sm-3 col-form-label">Введите цену:</label>
                <div class="col-sm-8">
                    <input required type="number" class="form-control" name="price" placeholder="Цена" value="{{price}}" >
                </div>
            </div>
            <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
            <button type="submit" class="btn btn-primary">
                Обновить
            </button>
            <a href="/admin" class="ml-3">Отменить</a>
        {{/autos}}
    </form>
</div>


</body>
</html>